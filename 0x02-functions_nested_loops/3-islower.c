#include "main.h"

/**
*_islower - checks for lower case letter
*@c: is the int that will use for the argument ofbthe function
*Return 0 always
*/

int _islower(int c)
{

	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}

	else

	return (0);

}
