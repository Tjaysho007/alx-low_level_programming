#include <stdio.h>
/**
* main-Entry point
* Descrotion: the code prints a - z
* Return: Always (0) (success)
*/

int main(void)
{
	char low;

	 	for (low = 'a'; low <= 'z'; low++)

		putchar(low);
	        putcahr('\n');
	return (0);
}
